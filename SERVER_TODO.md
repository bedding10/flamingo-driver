# SERVER_TODO — ما ينقص الخادم لإكمال تصميم Stitch

هذا الملف هو المصدر الوحيد لكل ما يحتاجه تطبيق السائق من الخادم ولم يوجد بعد.
كل بند مكتوب هنا لأن شاشة حقيقية في التصميم تنتظره، وليس كأمنية عامة.

قاعدة اتبعناها في كل الشاشات: **لا نختلق حقلًا ولا رقمًا**. إن لم يوفّره الخادم
فالشاشة تُبنى بالتصميم الكامل وتظهر حالة فارغة صريحة، حتى لا يرى السائق ولا
الموظف المراجع بيانات وهمية. لذلك كل سطر هنا يقابل حالة فارغة موجودة في الكود.

المرجع الحالي لما هو **موجود** فعلًا: `DRIVER_API_MAPPING.md`.

---

## 1) التسجيل والحساب (أولوية عالية — تعيق التسجيل)

| ما نحتاجه | لماذا | مقترح |
| --- | --- | --- |
| علم `hasPassword` في `GET /auth/me` أو `GET /driver/me` | الخطوة الثالثة في التسجيل تعيّن كلمة مرور عبر `POST /auth/password/change`، ولا توجد أي طريقة لمعرفة هل عُيّنت. لذلك بوابة التسجيل **لا تفحص كلمة المرور إطلاقًا**، وإلا أُعيد السائق العائد إلى الاستمارة في كل إطلاق. | `hasPassword: boolean` |
| توضيح سلوك أول تعيين لكلمة المرور | التطبيق يرسل `newPassword` بدون `currentPassword` في أول مرة. نحتاج تثبيت أن هذا مقبول رسميًا وليس مصادفة. | توثيق `currentPassword` كاختياري عند عدم وجود كلمة مرور |
| لا يوجد حقل بريد إلكتروني للسائق | تصميم Stitch يعرض خانة «البريد الإلكتروني» في شاشة المعلومات الأساسية. أزلناها واستبدلناها بكلمة المرور لأن الخادم لا يملك مكانًا لتخزين البريد. | `email?: string` إن أُريدت الخانة لاحقًا |

## 2) الوثائق والتحقق

| ما نحتاجه | لماذا | مقترح |
| --- | --- | --- |
| حالة `EXPIRED` في `DocumentStatus` | شاشة الوثائق تعرض «منتهية الصلاحية» كحالة رابعة، والخادم يعرف `PENDING/APPROVED/REJECTED` فقط. الحالة الآن تُحسب محليًا من تاريخ الانتهاء. | إضافة `EXPIRED` + `expiresAt` في مخرجات الوثيقة |
| نوع وثيقة `VTC_PERMIT` | «رخصة النقل (VTC)» مطلوبة في التصميم ولا يوجد نوع لها، فهي حاليًا **تحتل خانة `ID_CARD`** (التسمية عربية والقيمة المرسلة `ID_CARD`). | `DocumentType.VTC_PERMIT` |
| وجهان لكل وثيقة (أمام/خلف) | تصميم رفع رخصة السياقة يطلب صورتين. الأنبوب الحالي يقبل ملفًا واحدًا لكل نوع. | `side: "FRONT" \| "BACK"` أو نوعان منفصلان |
| سبب الرفض كنص | عند `REJECTED` لا يعرف السائق ما الخطأ، فيعيد رفع الصورة نفسها. | `rejectionReason?: string` |
| صور المركبة | التصميم يطلب أربع صور خارجية وصورًا داخلية. لا حقول لها، فهي تمرّ حاليًا عبر أنبوب الوثائق العام. | `VEHICLE_PHOTO` + `slot: FRONT/REAR/LEFT/RIGHT/INTERIOR` |

## 3) فحص المركبة (`safety_inspection`)

لا يوجد أي واجهة على الخادم لهذه الشاشة: لا قائمة بنود، ولا نسبة إنجاز، ولا
حفظ. التصميم يعرض «2 من 5 مكتملة» وقائمة تحقق داخلية.

- `GET /driver/me/inspection` → بنود الفحص وحالة كل بند وتاريخ آخر فحص.
- `PATCH /driver/me/inspection/:itemId` → تعليم بند.

## 4) المحفظة والمال

| ما نحتاجه | لماذا | مقترح |
| --- | --- | --- |
| طلب سحب من السائق | يوجد `GET /payments/payouts/me` للقراءة فقط. شاشة السحب لا تستطيع إنشاء طلب. | `POST /payments/payouts` |
| تعبئة الرصيد | شاشة `top_up_wallet` كاملة التصميم وبلا واجهة. | `POST /wallet/topup` |
| تحويل الرصيد لسائق آخر | شاشة `transfer_balance`. | `POST /wallet/transfer` |
| حدث سوكيت لتغيّر الرصيد | الرصيد لا يتحدّث إلا بسحب اليد. | `wallet:updated` |
| حصيلة شهرية | `/driver/me/earnings` لا يوفّر مجموعة شهرية، وشاشة تحليل الأرباح تحتاجها. | `period=month` أو مدى تاريخي |

## 5) الخريطة والمناطق

| ما نحتاجه | لماذا | مقترح |
| --- | --- | --- |
| مناطق الطلب والخريطة الحرارية | `demand_heatmap_main` و`zone_demand_details` بلا أي مصدر بيانات. | `GET /geo/demand-zones` (مضلّع + مضاعف السعر + عدد الطلبات) |
| مناطق العمل المحفوظة | الخادم يدعم منطقة واحدة لمدة 12 ساعة فقط، والتصميم يفترض قائمة. **أُخرجت الشاشة من النطاق بقرارك.** | قائمة مناطق بلا سقف زمني |
| ملاحة خطوة بخطوة | `POST /geo/directions` يعطي المسار فقط، فبانر الملاحة يعرض المسار والوقت والمسافة دون تعليمات. | خطوات مع نص وأيقونة لكل منعطف |

## 6) التفاوض على السعر

عروض السعر موجودة (`/driver/fare-offers`)، لكن التفاوض ناقص من جهة الراكب:

- حدث سوكيت لعرض مقابل من الراكب: `ride:counter_offer`.
- حدث لرفض/انتهاء العرض من جهة الراكب: `ride:offer_rejected`.
- بدون هذين الحدثين تبقى شاشة الانتظار تستطلع كل 15 ثانية بدل التحديث الفوري.

## 7) السمعة والتقييم

- توزيع النجوم (كم تقييم بخمس نجوم، أربع…) لشاشة `rating_overview`.
- وسوم التقييم (نظافة، قيادة هادئة…) لشاشة تفصيل المراجعة.
- مستوى السائق ورتبته: يوجد حدث `profile:level`، لكن لا نقطة نهاية تقرأ
  المستوى الحالي وشروط المستوى التالي (`status_levels_benefits`).

## 8) شاشات مبنية بالتصميم وحالة فارغة (بانتظار الخادم)

هذه بُنيت بصريًا بقرارك، وكلها تنتظر واجهة:

| الشاشة | الواجهة المقترحة |
| --- | --- |
| لوحة الصدارة | `GET /driver/leaderboard` |
| متتبّع المكافآت / الأهداف اليومية | `GET /driver/me/rewards`, `/goals` |
| الأوسمة والإنجازات | `GET /driver/me/badges` |
| الإحالات وسجلها | `GET /driver/me/referrals` + رمز الإحالة |
| عروض الشركاء وخصم الوقود | `GET /partners/offers`, `POST /partners/offers/:id/redeem` |
| جوائز الأسبوع / ملخص الموسم | `GET /driver/me/season` |
| مركز الإشعارات | لا يوجد إلا `/notifications/devices` للتسجيل؛ نحتاج `GET /notifications` مع «مقروء» |

## 9) الدعم والقانوني

- مقالات المساعدة لشاشة `help_support` (حاليًا نص ثابت في التطبيق).
- `/legal-documents/*` **موجود على الخادم ولم تستهلكه الشاشة القانونية بعد**؛
  ربطها يجعل الشروط وسياسة الخصوصية قابلة للتحديث دون إصدار جديد.

## 10) واجهات لم تعد مستخدمة

أزلنا نظام الاستغاثة بالكامل من التطبيق بقرارك، فهذه لم يبقَ لها مستهلك:

- كل نقاط الاستغاثة/الطوارئ ومسارات جهات الاتصال الطارئة.
- شاشة `emergency_sos` في التصميم **لن تُبنى**.

كما أن تتبّع الموقع صار في المقدمة فقط بعد إزالة رسالة الإفصاح، فقد ينقطع بث
الموقع عند إطفاء الشاشة. إن أُريد تتبّع في الخلفية لاحقًا فلا بد من إعادة رسالة
الإفصاح لتوافق متجر Play.
